[pytest]
markers =
    integration: marks tests as integration tests (may require network access)
    gpu: requires CUDA support
    slow: tests that take >5s locally
    optdeps: optional dependency required (SimpleITK, panopticapi, skimage, sklearn)
    unit: unit tests (fast, deterministic, single-process)
    concurrency: tests focused on multi-process/thread scenarios